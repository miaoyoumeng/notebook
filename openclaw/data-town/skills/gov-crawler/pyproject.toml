[project]
name = "gov-crawler"
version = "1.0.0"
description = "政府官方数据抓取，并提取其中的指标"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.1.29",
    # "python-dotenv>=1.0.0",
    # "pyyaml>=6.0.3",
    # "tzdata>=2024.1",
    # "javalang>=0.13.0"
]

[tool.ruff]
line-length = 100
