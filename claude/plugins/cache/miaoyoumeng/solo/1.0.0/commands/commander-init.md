---
description: 初始化项目工作环境。
allowed-tools: ["Bash(uv run python *)", "Read", "Glob", "Write", "AskUserQuestion", "Skill"]
---

1. 执行shell命令

```bash
uv run python ${CLAUDE_PLUGIN_ROOT}/scripts/init.py --workspace=<当前工作目录> 
```
