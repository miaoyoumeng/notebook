# kind: Deployment
# apiVersion: apps/v1
# metadata:
#   namespace: {{ $.Release.Namespace }}
#   name: {{ $.Values.apollo.name }}-apollo-admin-server
#   labels:
#     app: {{ $.Values.apollo.name }}-apollo-admin-server
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: {{ $.Values.apollo.name }}-apollo-admin-server
#   strategy:
#     rollingUpdate:
#       maxSurge: 1
#       maxUnavailable: 1
#     type: RollingUpdate
#   template:
#     metadata:
#       labels:
#         app: {{ $.Values.apollo.name }}-apollo-admin-server
#     spec:
#       affinity:
#         podAntiAffinity:
#           preferredDuringSchedulingIgnoredDuringExecution:
#           - weight: 100
#             podAffinityTerm:
#               labelSelector:
#                 matchExpressions:
#                 - key: app
#                   operator: In
#                   values:
#                   - pod-apollo-admin-server
#               topologyKey: kubernetes.io/hostname
      
#       volumes:
#         - name: config
#           configMap:
#             name: {{ $.Values.apollo.name }}-apollo-admin-server-configmap
#             items:
#               - key: application.properties
#                 path: application.properties

#       containers:
#         - image: apolloconfig/apollo-adminservice:2.0.1
#           securityContext:
#             privileged: true
#           imagePullPolicy: IfNotPresent
#           name: container-apollo-admin-server
#           ports:
#             - protocol: TCP
#               containerPort: 8090
          
#           volumeMounts:
#             - name: config
#               mountPath: /apollo-adminservice/config/application.properties
#               subPath: application.properties
#           env:
#             - name: APOLLO_ADMIN_SERVICE_NAME
#               value: "service-apollo-admin-server"
          
#           readinessProbe:
#             tcpSocket:
#               port: 8090
#             initialDelaySeconds: 10
#             periodSeconds: 5
          
#           livenessProbe:
#             tcpSocket:
#               port: 8090
#             initialDelaySeconds: 120
#             periodSeconds: 10
          
#       dnsPolicy: ClusterFirst
#       restartPolicy: Always
